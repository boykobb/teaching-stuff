%----Подреждане на две числа във възходящ ред---------------
/sort2 {
  2 copy gt {exch} if
} def

%----Отделяне на най-малък елемент (рекурсивно)-------------
/find-min {   % [ [ ... -> [ ... [ min
  counttomark 1 gt {
    sort2
    counttomark 1 add 1 roll
    find-min
  } if
} def

%----Подреждане на числа чрез отделяне на най-малък елемент (рекурсивно)
/min-up-sort {   % [ [ ... -> [ ...
  counttomark 1 eq {
    exch pop
  } {
    counttomark 1 add mark exch 1 roll
    find-min exch pop
    counttomark 1 add 1 roll
    min-up-sort
  } ifelse
} def

%----Пример-------------------------------------------------
[ [ 3 6 3 6 8 1 3 min-up-sort pstack

quit
